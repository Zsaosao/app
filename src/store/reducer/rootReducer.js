const init = {
    users: [
        { id: 1, name: 'dev' },
        { id: 2, name: 'frontend' },
    ],
};

function rootReducer(state = init, dispatch) {
    return state;
}

export default rootReducer;
